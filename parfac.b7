let parfac = mfix fac: []Int -> Int => n: Int =>
    let box u = n in
        if u < 2 then
            u
        else
            let box m = box (fac (box (n - 1))) in (n * m)

let main = _: () =>
    let x = parfac (box 20) in x
